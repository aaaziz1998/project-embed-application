# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'photo-library-mirai' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for photo-library-mirai
  pod "SkeletonView"
  pod 'JGProgressHUD'

  target 'photo-library-miraiTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'photo-library-miraiUITests' do
    # Pods for testing
  end

end
